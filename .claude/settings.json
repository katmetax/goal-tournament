{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "npx tsc --noEmit && npx eslint . && npx oxfmt src/ && npx vitest run" }
        ]
      }
    ]
  }
}
